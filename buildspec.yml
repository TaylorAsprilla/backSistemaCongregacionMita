version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16.18.0
    commands:
      - npm install -g typescript
      - npm install
  build:
    commands:
      - tsc

artifacts:
  type: zip
  files:
    - package.json
    - package-lock.json
    - "build/**/*"
  discard-paths: yes
